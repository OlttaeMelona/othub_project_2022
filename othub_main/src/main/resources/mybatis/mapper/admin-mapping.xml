<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="admin.AdminDAO" >

<!-- 관리자 로그인 -->
<select id="adminLogin" parameterType="admindto" resultType="admindto">
	select * from admin where a_id=#{a_id}	and a_pw=#{a_pw}
</select>

<!-- 관리자 정보 조회 -->
<select id="adminInfo" parameterType="admindto">
	select * from admin where a_id=#{a_id}
</select>

<!-- 관리자 정보 수정 -->
<update id="updateAdmin" parameterType="admindto" >
	update admin set a_id=#{a_id}, a_pw=#{a_pw}, a_name=#{a_name)
</update>

<!-- 관리자 삭제 -->
<delete id="deleteAdmin" parameterType="String">
	delete from admin where a_id=#{a_id}
</delete>


</mapper>

